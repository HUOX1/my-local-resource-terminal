# 本地资源终端开发交接日志 · 冻结快照

更新时间：2026-08-19（UTC+8）  
第一阶段冻结稳定基线：**v0.3.4.7**  
当前第二阶段冻结开发版：**v0.4.2.4**  
当前产品名：**本地资源终端**  
历史内部名：`local-movie-manager` / `LocalMovieManager`（兼容标识，继续保留）  
当前状态：**软件开发临时冻结；等待 UI / Motion 表达稿学习完成后再继续**

---

# 0. 本文件用途

本文件是 2026-08-19 的最新开发冻结快照，用于新对话 / 新开发助手继续开发时快速恢复上下文。

它取代旧日志里“当前版本 / 当前路线”的描述；旧日志仍可作为 v0.3.x ～ v0.4.1.9 的历史细节参考，但**当前状态以本文件为准**。

新对话继续开发时，建议至少提供：

1. 本文件；
2. `本地资源终端_v0.4.2.4_ProMotionDetail_完整源码.zip`；
3. 如要做差分升级，再提供最新更新覆盖包。

推荐新对话开场：

> 继续开发“本地资源终端”。请先完整阅读 `本地资源终端_开发交接日志_2026-08-19_冻结快照_v0.4.2.4.md`。v0.3.4.7 是第一阶段冻结功能基线；当前第二阶段开发暂时冻结在 v0.4.2.4。不要重新设计成熟业务逻辑。下一步已经确定但尚未开始的是海报墙底层布局引擎重写、主轴梯形把手结构修正、档案页真实 Geometry Reflow，以及之后的滚动手感专项。Identity 的新品牌动画尚处于外部 Motion 稿学习阶段，本日志故意不记录其具体设计。

---

# 1. 产品定位

当前产品定位：

> **私人本地数字收藏终端**

当前只有两个正式领域：

- Movie / 影片
- Game / 游戏

架构继续坚持：

> **平行领域模块 + 共享应用外壳**

不要因为产品名是“资源终端”就提前抽象音乐、漫画、万能 `ResourceRecord` 或统一 `resources` 表。

它不是：

- Jellyfin / Plex 替代；
- Web 服务；
- Steam / GOG / Epic 聚合器；
- 商店；
- 社交平台；
- 游戏 Overlay；
- 模拟器前端总控平台。

---

# 2. 阶段划分

## Stage 1：功能与数据可靠性

冻结基线：**v0.3.4.7**。

已经由真实 Windows 使用和破坏性测试确认成熟，业务逻辑原则上不重新开放设计。

核心能力包括：

- Movie / Game 平行领域；
- JSON 永久档案；
- SQLite 可重建索引；
- 游戏启动、计时、Session；
- 影片外部播放器启动；
- 备份 / 恢复；
- 海报缓存 / GIF 预览；
- 单实例；
- 模拟器与普通 PC 游戏真实设备验证。

## Stage 2：视觉、皮肤、内容页与 Motion

当前开发线已经推进到：**v0.4.2.4**。

当前阶段重点不是继续加业务功能，而是：

- Flat Pro / Flat Pro Light；
- 独立档案内容页；
- Identity Shell；
- 主轴与内容区；
- Motion；
- 响应式布局；
- 海报墙视觉连续性。

---

# 3. 强约束与开发偏好

这些约束继续生效。

## 3.1 真实使用优先

用户明确偏好：

> **边用边测问题，不能光靠幻想就徒增工作量。**

因此：

- 真实 Windows 截图和实机手感优先；
- Bug 先查根因再修；
- 不为了可能存在的问题提前造大系统；
- UI 动画必须以实机“是否顺”为最终判断；
- 不用生成式概念图替代真实代码实现。

## 3.2 TDD / 验证

当前开发习惯：

1. 独立复制工作目录，不在原包直接改；
2. 新行为先写失败测试；
3. 最小实现；
4. 全量 `pytest`；
5. `python -m compileall -q app tests`；
6. 打包后重新解压验证；
7. 更新覆盖包实际覆盖到上一版后，对运行文件做 missing / extra / mismatch 比对；
8. ZIP 做完整性检查。

当前环境**没有 Git 仓库**，不要声称 commit / branch / worktree。

## 3.3 交付规则

普通版本只交付：

- 更新覆盖包；
- 完整源码包。

更新覆盖包：

- 只放真正需要覆盖 / 新增的运行文件；
- 不再累计 `升级说明_v*.txt`；
- 不放交接日志；
- 不放测试文件。

交接日志只在：

- 重大里程碑；
- 数据结构 / 路线变化；
- 切换对话；
- 当前这种“暂停开发冻结”节点

集中更新。

---

# 4. 技术栈与兼容身份

当前技术栈：

- Python 3.11+
- PySide6 Widgets
- SQLite
- JSON 永久元数据
- Pillow
- FFmpeg / ffprobe
- Windows 桌面程序

当前 `pyproject.toml`：

- project name：`local-movie-manager`
- version：`0.4.2.4`
- Python：`>=3.11`

Windows 10 实机环境：

- Windows 10 Pro 22H2
- Build 19045.6466

启动方式：

- `run_windows.vbs`：日常静默启动；
- `run_windows_debug.bat`：调试；
- 当前仍不是正式单文件 EXE。

内部标识 `local-movie-manager` / `LocalMovieManager` 不要随便改，用户配置目录和历史数据可能依赖它。

---

# 5. 数据与备份当前状态

## 5.1 数据原则

- JSON = 永久档案；
- SQLite = 索引 / 查询，可重建；
- 删除影片本体 ≠ 删除影片档案；
- 删除游戏本体 / EXE ≠ 删除游戏档案；
- 失联档案继续保留；
- 海报 / GIF / Hero 等视觉资产有独立缓存与备份策略。

## 5.2 当前数据库

当前 SQLite schema version：**6**。

关键后续字段：

- Movie：`folder_id`
- Movie：`total_play_seconds`
- Game：`archive_media_path`
- Game：`folder_id`
- Game：`total_play_seconds`

注意：Movie 表历史字段 `watched / favorite / play_count` 仍可能存在于兼容数据结构中，但**当前 UI 已不再把收藏 / 已观看 / 播放次数作为影片价值表达**。不要因为字段还在数据库里就重新把按钮加回来。

## 5.3 备份

当前备份格式：**v4**。

支持旧版本：1 / 2 / 3 / 4。

当前备份包含：

- Movie / Game 档案；
- Identity；
- visual assets；
- 游戏档案 Hero；
- 一级分类文件夹。

旧备份恢复继续兼容。

恢复 SQLite 时必须延续 v0.3.4.7 已修好的原则：**先关闭活跃数据库连接，再替换文件**，不要重新引入 WinError 5 文件锁问题。

---

# 6. Stage 1 冻结业务规则

这些成熟规则不要在 UI 阶段顺手重写。

## Movie

- 海报墙：固定展示宽度；
- 高度按源图自然比例；
- 不裁切；
- 不补黑边 / 白边；
- 海报墙不显示永久文字；
- 外部播放器播放；
- 当前影片累计播放时间为 `total_play_seconds`；
- 只有能取得实际播放器进程生命周期时才累计真实播放时长；
- 不用“片长 × 次数”制造假时长。

## Game

- Game Name、Launch EXE、Timing EXE 是核心启动配置；
- rating 保留；
- `description` = 作品介绍；
- `notes` = 我的记录；
- Timing EXE 真出现才开始计时；
- 只统计终端发起的 Session；
- 游戏运行时终端尽量消失；
- 不做 Overlay / 注入 / 输入 Hook；
- `total_play_seconds` 是当前主要喜爱 / 使用表达；
- “游玩次数”不再作为档案页核心信息。

---

# 7. 一级分类文件夹

自 v0.4.1.9 起，Movie / Game 都有一级虚拟分类文件夹。

规则：

- 只允许一级；
- 不做嵌套；
- 不移动真实视频 / 游戏文件；
- 只记录组织元数据；
- 资源可移动到分类或“未分类”；
- 文件定义存放于 `Data/collections/folders.json`；
- 档案记录保存 `folder_id`。

未来电视剧如果实现 Season / Episode，应属于系列档案内部层级，不要拿分类文件夹做多级目录。

---

# 8. Identity Shell 当前状态

Identity 已经成为正式主壳的一部分。

规则：

- 本地单身份；
- 用户名必填；
- 头像 / frame 可选；
- 无密码；
- 无邮箱；
- 无在线账号；
- 无多用户；
- 无云同步 / 社交；
- 每次启动先停在全窗口 Identity Entry；
- 点击身份后进入主界面；
- 进入后身份缩到左上主轴；
- 点击左上身份可编辑；
- Identity 独立于 Movie / Game DB；
- Identity Entry 后自动扫描。

旧身份数据可能继续存在于 `LocalMovieManager` 用户配置目录，这是兼容行为。

**重要冻结说明：**

目前正在学习 / 制作新的 Identity → Terminal Motion 表达稿。该动画具体创意本日志**故意不记录**，当前软件也不要根据口头描述提前实现。等用户完成动画表达稿后再单独写入设计 / 日志。

---

# 9. Theme / Skin 当前状态

早期 Win10 外窗玻璃方案已经正式放弃。

不要重新开启：

- 外层透明圆角窗；
- Win10 fake glass；
- ghost material；
- PyWinRT / DWM 玻璃折腾；
- 透明 frameless 外窗实验。

当前接受：

> **矩形不透明 frameless window + 自定义软件 chrome**

Theme Registry 是唯一主题来源。

截至 v0.4.2.4，正式主题只保留：

- `flat_pro` → Flat Pro
- `flat_pro_light` → Flat Pro Light

旧 ID 只做迁移：

- `flat_dark` → `flat_pro`
- `flat_light` → `flat_pro_light`

Flat Pro / Pro Light：

- 共用布局；
- 共用组件；
- 共用 Motion；
- 共用尺寸 / 间距 /状态逻辑；
- 主要通过 token 替换颜色。

不要再让 Light / Dark 分叉成两套不同功能实现。

---

# 10. Flat Pro 主壳当前冻结状态

## 10.1 自定义 Titlebar

当前包含：

- 产品名“本地资源终端”；
- 可见版本号；
- 最小化；
- 最大化 / 还原；
- 关闭；
- `startSystemMove`；
- 双击 title 最大化；
- Win10 frameless 四边 / 四角 resize。

外层窗口继续保持矩形、不透明。

## 10.2 主轴

当前主轴只允许两种状态：

- 展开：头像 + 名字 + 影片 / 游戏 / 设置图标与文字；
- 最小：头像 + 影片 / 游戏 / 设置图标。

v0.4.2.2 已实现：

- 用户名 / 分组标题 / 导航文字淡入淡出；
- 图标随着宽度移动；
- 主轴缩放 Motion。

v0.4.2.3 起，不再允许停在任意中间宽度。

## 10.3 当前梯形把手实现

v0.4.2.4 已把胶囊按钮改为梯形按钮，但**当前实现尚未最终满意**。

当前已知问题和下一步已定方案见第 18 节。

---

# 11. Movie 独立档案页当前状态

影片档案已经从旧弹窗升级为内容区独立页面。

当前主流程：

- `←` 返回；
- 大封面；
- 标题 / code；
- rating；
- actors；
- series；
- studio；
- release date；
- tags；
- subtitles；
- notes；
- media info / path；
- 累计播放时间；
- Play 主按钮。

v0.4.1.10 起，编辑已经直接融合到档案页：

- 标题直接编辑；
- code 直接编辑；
- cover key 直接编辑；
- actors / series / studio / release date / tags 直接编辑；
- 星级直接点击；
- notes 直接编辑；
- 封面更换 / 打开目录 / relink / 删除档案在页面内完成。

旧 `movie_detail.py` 仍保留兼容 / 历史，但不再是主流程入口。

## 11.1 影片观看语义

v0.4.1.13 起，档案页不再显示：

- 收藏；
- 已观看；
- 播放次数；
- 首次播放；
- 最后播放。

当前只强调：

> **累计播放 X.X 小时**

影片右键菜单里的“收藏 / 已观看”残留已经在 Motion 阶段清除。

## 11.2 影片封面

档案页封面已改为：

- 固定展示宽度约 280px；
- 高度按源图比例自然计算；
- 不放进固定 220×320 框；
- 不补黑边；
- 不裁切。

---

# 12. Game 独立档案页当前状态

Game 档案已从弹窗升级为内容区独立页面。

当前包含：

- `←` 返回；
- Hero；
- title；
- rating；
- 累计游玩时间；
- 安装 / 失联状态；
- 启动按钮；
- 作品介绍；
- 媒体；
- 我的记录；
- 档案数据；
- Session / play stats；
- 运行配置。

Hero 媒体优先级：

1. 手动 archive media；
2. preview GIF；
3. screenshot directory 中图片 / GIF；
4. theme background。

不支持视频 Hero，避免本地数据体积扩张。

`archive_media_path` 已进入档案、SQLite、visual backup。

## 12.1 Game 编辑已融合

v0.4.1.14 起，旧“编辑档案”弹窗不再是主流程。

独立页已直接承载：

- title；
- rating；
- series；
- developer；
- publisher；
- release date；
- tags；
- description；
- notes；
- launch EXE；
- timing EXE；
- args；
- working directory；
- screenshot directory；
- cover；
- GIF。

## 12.2 游戏游玩语义

档案页不再强调“游玩次数”。

主要表达：

> **累计 X.X 小时**

历史 Session 仍可作为档案数据存在，但“误触启动也算一次”的次数不再作为喜爱程度表达。

---

# 13. 失联档案

v0.4.1.16 起：

- Movie 视频本体不存在 → 海报灰化；
- Game `launch_exe` 不存在 → 海报灰化；
- Game 失联状态不播放彩色 GIF hover；
- 档案本身继续保留；
- 文件恢复后，刷新 / 扫描后恢复正常颜色。

这是当前明确的产品语义：

> **档案是一份收藏记录，不因为当前内容文件丢失就自动消失。**

---

# 14. 海报墙当前状态

## 14.1 已冻结视觉规则

Movie / Game 海报墙继续坚持：

- 海报固定展示宽度；
- 图片按自然比例；
- 不裁切；
- 不补边；
- 不在海报下永久显示文字；
- 图片缓存 / resize 性能缓存必须保留。

双击语义：

- Movie 双击 → 影片档案；
- Game 双击 → 游戏档案；
- 不再双击直接 Play / Launch；
- 真正播放 / 启动在档案页主按钮。

## 14.2 当前实现历史

v0.4.1.11：

- `ScrollPerPixel`；
- 28px 滚动步进；
- 海报墙慢浏览。

v0.4.1.14 ～ v0.4.1.16：

- 尝试“填满右侧”布局；
- 处理 resize 时右侧明显空位；
- 出现强制换列、闪烁问题；
- 后续通过 debounce / spacing 算法缓解，但没有真正解决底层决策权问题。

v0.4.2.0：

- Motion Foundation；
- hover 轻微放大 / 上浮；
- 档案页进入 / 返回短过渡；
- 海报位置过渡实验。

v0.4.2.1：

- 海报尺寸继续固定；
- 尝试“间距优先适配 + 列数阈值 + easing”；
- 但当前真实实机仍出现“缺位 / 填满之间反复切换”。

## 14.3 当前已知根因

已确认：问题不是简单的动画时长 / easing。

当前底层仍依赖：

- `QListView`
- `IconMode`
- `Wrapping`
- `ResizeMode.Adjust`

当前自定义算法虽然会计算列数 / spacing，但**最终实际换行仍由 QListView 自己决定**。

因此会出现：

> 我们的“应该是 N 列”与 QListView 自己的“此刻到底能不能放 N 张”在临界宽度不一致。

窗口连续缩放时就会在：

- 右侧缺位；
- 又突然补满；
- 下一次又重新换行

之间来回变化。

此问题已经明确进入“底层布局引擎重写”级别，不再继续用 spacing / debounce / animation 参数补丁解决。

---

# 15. Motion 当前状态（仅记录已经进入软件的部分）

**注意：本节只记录已经实现的普通 UI Motion。当前正在学习 / 设计的 Identity 新品牌动画不写入本节。**

## v0.4.2.0 — Motion Foundation

Flat Pro 开始拥有独立 Motion 层：

- 海报 hover；
- 海报重排位置过渡实验；
- 档案页进入 / 返回短淡入 + 位移；
- 滚轮惯性实验；
- Flat Pro 才启用；当时旧 Flat Dark / Light 保持静态。

## v0.4.2.1 — 海报墙排位重写实验

- 固定海报尺寸；
- 间距优先；
- 列数阈值；
- `OutQuint`；
- 动画中继续 resize 时尝试从当前显示位置接续。

实机确认：仍未从底层解决 QListView 换行决策问题。

## v0.4.2.2 — 主轴动态

- 用户名淡入淡出；
- 分组标题淡入淡出；
- 导航文字淡入淡出；
- 图标位置随宽度平滑移动。

## v0.4.2.3 — Pro Shell Consolidation

- 主轴改为“展开 / 最小”严格两态；
- Flat Dark / Flat Light 删除；
- 新增 Flat Pro Light；
- 档案窄窗口横向滚动条隐藏；
- 档案窄屏开始做响应式重排。

## v0.4.2.4 — Pro Motion Detail

当前已经实现：

- 梯形主轴把手第一版；
- Identity Entry → 主界面短淡入 / 位移（临时普通 Motion，未来品牌动画可能替换）；
- 搜索 / 分类工具弹层短淡入 + 上浮；
- Movie / Game 右键菜单统一 Pro 弹出 Motion；
- Game Hero 切换 cross-fade；
- Movie / Game 直接编辑状态短淡入；
- 星级点击轻微反馈；
- Movie / Game 档案窄屏方向切换时使用旧页面快照做淡出过渡。

实机反馈：

- 菜单 / hover / Hero / 编辑等 Motion 整体低调，方向正确；
- 主轴文字 / 图标 Motion 没有明显问题；
- 档案响应式动画缺乏“内容自身移动感”，需要后续重做；
- 海报墙排位问题仍是当前最大结构问题。

---

# 16. 当前 UI 冻结 / 不要随便继续磨的区域

除非真实设备反馈新问题，否则以下区域暂时不要继续无目的微调：

- Flat Pro 左侧导航卡片视觉；
- 底部极简工具区；
- 自定义 titlebar；
- Movie / Game 独立档案主信息架构；
- Inline edit 的基本方向；
- Movie / Game 双击进入档案；
- Movie / Game 失联灰化语义；
- Flat Pro / Flat Pro Light 双主题同构架构；
- 菜单 / hover / Hero 当前低调 Motion 风格。

当前工作应集中在真正仍有结构性问题的区域。

---

# 17. 当前版本验证基线

当前冻结开发版：**v0.4.2.4**。

v0.4.2.4 最终成品 ZIP 重新解压验证：

- `357 passed, 4 skipped`
- `python -m compileall -q app tests` → OK
- 更新覆盖包套到 v0.4.2.3 后运行文件比对：
  - `0 missing`
  - `0 extra`
  - `0 mismatch`
- 两个 ZIP 完整性检查通过。

当前工作基准应使用：

> `本地资源终端_v0.4.2.4_ProMotionDetail_完整源码.zip`

第一阶段回退参照继续保留：

> `v0.3.4.7`

---

# 18. 已经讨论确定、但尚未开始实现的下一批内容

**本节是当前最重要的“冻结后待办”。这些内容已经讨论清楚，但当前开发已经暂停，尚未开始改代码。**

## 18.1 v0.4.3.0 — Poster Wall Engine

目标：从底层解决海报墙“缺位 / 填满 / 临界换列反复”的问题。

当前推荐方案：

1. 不再让 `QListView + Wrapping` 决定最终海报行列；
2. 自己实现海报墙目标布局计算；
3. 每个海报明确拥有唯一 `targetRect / target position`；
4. 海报尺寸继续固定，不随窗口缩放；
5. 窗口变化主要调整海报间距和目标坐标；
6. 增 / 减一列必须由明确阈值决定；
7. 增加 hysteresis / 滞回区，避免临界宽度来回换列；
8. Motion 层只负责：当前位置 → 目标位置；
9. Layout 层和 Motion 层分离，不能再互相争夺 geometry；
10. 保留现有：
   - 图片缓存；
   - GIF hover；
   - 右键；
   - 双击；
   - 选择；
   - 失联灰化；
   - Movie / Game 两领域行为。

这次允许推翻现有海报排位实现，有必要可以重写，不要继续给 QListView wrapping 打补丁。

### 尚需在开工前确认的一个视觉选择

海报墙新引擎需要最终确认：

- A：整墙始终居中；
- B：第一张始终保持固定左侧起点，剩余空间主要分配到海报间距。

该选择尚未最终回答，不要擅自决定。

## 18.2 主轴梯形把手结构修正

v0.4.2.4 的把手形态已接近方向，但当前问题：

- 仍画在 `QSplitterHandle` 上；
- 因此视觉上仍处于“分界线上”；
- 梯形略高 / 略大。

下一步已定：

1. 把梯形按钮从 splitter handle 中拆出来；
2. 改成独立悬浮控件；
3. 展开状态：
   - 梯形整体位于**主轴区域内部**；
   - 箭头指向主轴；
4. 最小状态：
   - 梯形整体位于**内容区内部**；
   - 箭头指向内容区；
5. 大小比当前再小一点；
6. 形状更扁；
7. 参考尺寸约：
   - 宽 13～14px；
   - 高 34～36px；
8. 继续保持极低视觉存在感。

## 18.3 v0.4.3.1 — 档案页真实 Geometry Reflow

当前 v0.4.2.4 的档案窄屏 Motion 是：

> `QBoxLayout.setDirection()` 先瞬间换布局 → 旧页面快照淡出。

实机反馈：

- 有动画；
- 但没有“动感”；
- 子内容卡片本身没有移动；
- 看起来仍然是瞬间变结构后淡入淡出。

下一步已确定改成真正的响应式 geometry animation：

1. 不再用“旧页面整张快照淡出”作为主要响应式动画；
2. 每个档案子内容框 / card 是独立可移动单元；
3. 当前 geometry 与目标 geometry 分离；
4. 窗口变窄时，卡片像“大方框气泡”一样被空间挤动；
5. 左右布局 → 上下布局时，卡片本身移动 / 拉伸 / 收缩到新位置；
6. 窗口正常小幅 resize 时只做柔和尺寸跟随；
7. 真正跨布局模式时才做明显 reflow；
8. Movie / Game 共用一套 `AnimatedResponsiveLayout` / 类似独立机制；
9. 横向滚动条继续永久隐藏；
10. 不能因为动画导致内容裁切或不可访问。

建议顺序：

- 先做 v0.4.3.0 海报墙底层布局；
- 再做 v0.4.3.1 档案 Geometry Reflow。

## 18.4 Scroll & Motion Tuning

滚动手感专项暂时继续后置。

历史上已经试过：

- QPropertyAnimation stop/restart → 卡；
- 单 timer 速度累积 → 实机仍不理想；
- 之后暂时回退稳定直接滚动。

未来单独做：

- 鼠标滚轮短惯性；
- 连续输入不顿；
- 松手自然减速；
- 不漂；
- 触控板尽量保留系统原生像素滚动；
- Movie / Game 海报墙 / 档案页手感统一；
- 最后统一 Motion duration / easing。

不要把滚动专项塞进海报墙底层重写或档案 reflow 一起调。

---

# 19. 当前明确不写入本日志的 Motion 设计内容

用户当前正在单独学习 / 制作 UI Motion 表达稿。

这一部分**暂不进入开发日志**：

- 不记录具体动画创意；
- 不记录分镜、节奏、关键帧、参考风格；
- 不根据此前聊天里的零散口头描述提前实现；
- 等用户把 Motion 学习 / 表达稿告一段落后，再单独补写正式设计与冻结规则。

因此，本文件只冻结软件代码与已经确定的工程待办，不冻结尚未完成表达的 Motion 创意。

# 20. 当前软件开发冻结规则

从本文件生成起：

> **代码开发暂时冻结在 v0.4.2.4。**

冻结期间：

- 不主动继续做 v0.4.3.0；
- 不调滚动参数；
- 不继续加 Motion；
- 不重做 Identity；
- 不重打无意义小版本；
- 不更新 schema；
- 不扩展新领域。

除非用户中途发现必须立即修复的数据 / 启动 / 崩溃级真实 Bug，否则等待动画表达学习告一段落后再恢复开发。

恢复开发时优先顺序：

1. Poster Wall Engine；
2. 梯形把手结构修正（可和 1 合版）；
3. 档案真实 Geometry Reflow；
4. Scroll & Motion Tuning；
5. 再根据用户最终 Motion 稿决定 Identity 品牌动画。

---

# 21. 当前不要做的事

## 架构

- 不抽象第三领域；
- 不做万能资源表；
- 不把 Movie / Game 合成一套万能数据模型；
- 不重新开放 Stage 1 已成熟的核心业务。

## Windows 视觉

- 不重启 Win10 外窗玻璃实验；
- 不做透明圆角外窗；
- 不追 DWM / PyWinRT fake glass；
- 不用视觉 gimmick 破坏当前稳定 frameless chrome。

## Game

- 不做 Overlay；
- 不 Hook 输入；
- 不注入进程；
- 不主动截图；
- 不做成就系统；
- 不做重型后台监控。

## Movie

- 不重新加“收藏 / 已观看”作为主要 UI；
- 不用播放次数表达喜爱；
- 不做在线 scraper；
- 不凭空扩充 NAS / Jellyfin 式大量必填元数据。

未来如做离线元数据辅助，优先考虑：

- 从剪贴板整理资料；
- 本地 `.nfo` 导入；
- 用户确认后写入。

---

# 22. 最新一句话交接

> **本地资源终端当前冻结在 v0.4.2.4。Stage 1 业务继续以 v0.3.4.7 为成熟稳定基线；Stage 2 已完成 Flat Pro / Pro Light、Identity Shell、Movie / Game 独立档案页、一级分类、内联编辑、失联灰化和第一轮低调 Motion。当前最主要的未解决结构问题是海报墙仍被 QListView wrapping 掌握最终换行，导致 resize 时缺位 / 填满反复；下一步允许从底层重写 Poster Wall Engine。主轴梯形把手要从 splitter handle 脱离并根据展开 / 最小状态真正进入主轴区 / 内容区；档案页要从“整页快照淡出”升级为卡片自身 Geometry Reflow。软件当前暂停开发，等待用户完成 UI Motion 表达学习。Identity 的新品牌动画创意本日志故意不记录。**
