# 本地资源终端开发交接日志

更新时间：2026-08-19（UTC+8）  
第一阶段稳定/冻结基线：**v0.3.4.7**  
当前第二阶段开发版：**v0.4.1.8**  
当前产品名：**本地资源终端**  
早期产品名：本地影片管理器  
当前阶段：**Stage 1 冻结；Stage 2 已进入 Flat Pro / 皮肤体系实装与独立内容页迭代期**

---

# 0. 交接用途

本文件不仅是版本日志，也是新对话 / 新开发助手的**完整交接文件**。继续开发前应先完整阅读本文件，不要只看 README 或某个旧设计文档。

新对话建议同时提供：

1. 本文件 `本地资源终端_开发交接日志_2026-08-18.md`；
2. 最新完整包 `本地资源终端_MVP_v0.3.4.7_第一阶段冻结基线完整版.zip`；
3. 如用户在自己机器上又发现真实 Bug，再按当前实际安装目录为准做小补丁。

推荐新对话开场：

> 继续开发“本地资源终端”。请先完整阅读交接日志。v0.3.4.7 是第一阶段冻结功能基线，不要重新设计其成熟业务；当前第二阶段开发线已到 v0.4.1.8。继续开发时以最新完整源码为工作基准，保留 v0.3.4.7 作为功能回退参照。当前重点是 Flat Pro、皮肤体系和游戏独立档案内容页，真实 Windows UI 仍以用户实机反馈为最高依据。

**重要：** 当前已经不是单纯影片管理器，而是两个独立领域：

- 影片 Movie
- 游戏 Game

当前架构明确采用：

> **平行领域模块 + 共享应用外壳**

不要因为产品名叫“资源终端”就提前抽象音乐、漫画或万能 `ResourceRecord/resources` 表。

---

# 1. 产品定位与阶段划分

项目最初因用户认为 Jellyfin 等方案过重、不想要浏览器 / Web 服务而开始，目标是一个：

- Windows 原生桌面程序；
- 完全本地；
- 不开 Web 服务；
- 不依赖浏览器；
- 外部播放器播放影片；
- 永久保存影片档案；
- 后来增加本地游戏档案、启动和游玩时长。

当前产品定位：

> **私人本地数字收藏终端**

它不是：

- Jellyfin/Plex 替代；
- Steam/GOG/Epic 聚合器；
- 商店；
- 社交平台；
- RetroArch/模拟器前端；
- 游戏 Overlay 平台。

用户已把开发阶段明确理解为：

> **第一阶段 = 0 → 1：有没有、能不能用、数据是否可靠。**  
> **第二阶段 = 1 → 10：视觉、布局、流动、仪式感、身份与软件人格。**

截至 v0.3.4.7，第一阶段已由用户真实使用和破坏性测试确认完成，因此当前状态为：

> **Stage 1：完成，冻结。**  
> **Stage 1.5：日常使用中只修真实 Bug，不重新开放功能膨胀。**  
> **Stage 2：开始审美积累和设计。**

---

# 2. 用户开发偏好与工作原则

这些是强约束。

## 2.1 真实使用优先

用户明确说过：

> **边用边测问题，不能光靠幻想就徒增工作量。**

因此：

- 真实问题优先于设想功能；
- 不为了“可能以后有用”做大子系统；
- 新功能先定边界再写；
- Bug 先找根因再修；
- UI 性能等问题必须靠实际使用验证。

最近实际使用正是这种方式暴露并修复了：

- ffprobe/FFmpeg 控制台闪烁；
- 影片封面墙 resize 高 CPU；
- 游戏封面墙同类问题；
- SQLite 恢复文件锁；
- RPCS3 后台残留进程等。

## 2.2 小补丁版本

用户不希望版本号跳得太快。

当前习惯：

- 大功能：如 v0.3.0、v0.3.4；
- 小 UI/兼容/性能/Bug：`v0.3.4.1`、`.2`、`.3`……

当前最新：**v0.3.4.7**。

第一阶段冻结后，如果发现真实 Bug，优先继续：

- v0.3.4.8
- v0.3.4.9

而不是因为进入第二阶段就强行升级版本。

## 2.3 数据安全优先

- JSON = 永久档案；
- SQLite = 快速索引，可重建；
- 游戏本体删除 ≠ 游戏档案删除；
- 影片本体删除 ≠ 影片档案删除；
- 备份/恢复是核心可靠性功能；
- 原始封面图片不修改；
- 外部截图不复制、不删除。

## 2.4 游戏运行时尽量消失

已形成明确原则：

> **游戏开始前负责仪式感；游戏结束后负责档案；真正玩的时候尽量消失。**

因此不做：

- Overlay；
- 输入 Hook；
- 游戏注入；
- 软件主动截图；
- 成就 Hook；
- 重型后台监控。

---

# 3. 技术栈与兼容身份

技术栈：

- Python 3.11+
- PySide6 Widgets
- SQLite
- JSON 永久元数据
- Pillow
- FFmpeg / ffprobe
- Windows 桌面程序

`pyproject.toml` 当前：

```toml
[project]
name = "local-movie-manager"
version = "0.3.4.7"
requires-python = ">=3.11"
dependencies = ["PySide6>=6,<7", "Pillow>=10,<13"]
```

**不要随便改内部 package/application 标识。**

外部产品名已是“本地资源终端”，但历史内部名 `local-movie-manager` / `LocalMovieManager` 继续保留兼容，设置目录 / QStandardPaths 等可能依赖它。

Windows 启动：

- `run_windows.vbs`：日常静默启动；
- `run_windows_debug.bat`：显示控制台/Traceback 排错；
- 旧兼容启动文件仍可能存在。

当前还不是正式独立 EXE，典型链路：

```text
VBS → .venv\Scripts\pythonw.exe → python -m app.main
```

正式 EXE、图标、品牌、启动视觉属于第二阶段后续，不是第一阶段缺口。

---

# 4. 当前核心代码结构（v0.3.4.7）

```text
app/
├─ bootstrap.py
├─ main.py
├─ restart.py
├─ single_instance.py
├─ config/
│  ├─ data_dirs.py
│  └─ settings.py
├─ db/
│  ├─ database.py
│  └─ schema.sql
├─ models/
│  ├─ movie.py
│  ├─ game.py
│  └─ scan.py
├─ repositories/
│  ├─ movie_repository.py
│  └─ game_repository.py
├─ services/
│  ├─ catalog_service.py
│  ├─ discovery_service.py
│  ├─ scanner.py
│  ├─ metadata_service.py
│  ├─ cover_service.py
│  ├─ media_probe.py
│  ├─ player_service.py
│  ├─ viewing_service.py
│  ├─ giga_cover_cropper.py
│  ├─ backup_restore_service.py
│  ├─ subprocess_utils.py
│  ├─ game_catalog_service.py
│  ├─ game_metadata_service.py
│  ├─ game_asset_service.py
│  ├─ game_launcher.py
│  ├─ game_session_service.py
│  └─ screenshot_service.py
└─ ui/
   ├─ main_window.py
   ├─ settings_dialog.py
   ├─ movie_models.py
   ├─ movie_delegate.py
   ├─ movie_detail.py
   ├─ game_models.py
   ├─ game_delegate.py
   ├─ game_edit_dialog.py
   ├─ game_detail.py
   ├─ giga_cover_dialog.py
   ├─ manual_cover_crop_dialog.py
   ├─ batch_edit_helpers.py
   ├─ poster_layout.py
   └─ scan_worker.py
```

当前数据库 schema version：**3**。

v2 → v3 的关键迁移是游戏新增 `description`（界面名“作品介绍”）。

---

# 5. 数据目录与数据职责

当前概念结构：

```text
Data/
├─ library.db
├─ settings.json
├─ metadata/
│  ├─ movies/
│  │  └─ <movie>.json
│  └─ games/
│     └─ <game-uuid>.json
├─ game_assets/
│  ├─ covers/
│  └─ previews/
├─ state/
│  └─ active_game_session.json
├─ cache/
│  ├─ thumbnails/
│  ├─ generated_covers/
│  ├─ movies/
│  └─ games/
│     └─ screenshots/
└─ logs/
```

影片正式封面仍可使用用户配置的外部集中封面目录，不强制搬入 Data。

核心原则：

> **JSON / 永久资源文件 = 长期档案。**  
> **SQLite = 快速查询、索引和运行期层。**

用户已真实测试：

- 完全移走 `library.db`；
- 启动终端；
- `library.db` 自动重新生成；
- 没发现影片、游戏等数据损失；
- **不需要恢复备份。**

因此“SQLite 可从永久 JSON 重建”已经不是理论，而是实机验证通过。

---

# 6. Movie 领域：第一阶段稳定能力

影片模块经过长期使用，当前视为成熟。

能力：

- 多影片库根目录；
- 自定义库名；
- 启用/禁用；
- 启动扫描 + 手动重扫；
- 不做文件系统实时 watcher；
- 自动发现 + 手动编辑元数据；
- 视频删除后永久档案保留；
- 集中正式封面；
- JSON 永久档案；
- SQLite 快速索引；
- 外部播放器；
- 终端发起播放后记录观看事件；
- 排序、筛选、多选、批量编辑；
- 备份恢复；
- GIGA 封面处理。

主要字段：

- code/番号
- title
- actors
- series
- studio
- release_date
- tags
- rating
- favorite
- watched
- notes
- file path
- cover
- added_at
- media info
- play/viewing stats

## 6.1 海报墙规则：不要轻易改

用户经历过固定框裁切、Fit 补边等方案后，最终确认：

> **纯封面、固定显示宽度、高度按原图自然比例、不裁切、不补边、不永久显示标题/番号/评分。**

交互：

- 单击选择；
- 双击播放；
- 右键详情/管理。

第二阶段即使重新设计主壳，也不要无故把影片墙退回“统一固定卡片高度 + 大量文字”。

## 6.2 影片排序 v0.2.4

- added_at
- 番号
- 标题
- 发行日期
- 评分
- 最后观看
- 观看次数
- 升/降序

用户实测通过。

## 6.3 备份恢复 v0.2.5 起

设置内使用，后续已扩展游戏领域。详见第 11 节。

## 6.4 批量编辑 v0.2.6

Ctrl/Shift 多选：

- 加/删标签；
- 设置厂商/系列；
- 收藏/取消；
- 已观看/未观看。

用户实测通过。

## 6.5 GIGA 手动 Front 裁剪 v0.2.7

完整 DVD Wrap 结构：

```text
Back | Spine | Front
```

手动裁剪窗口：

- 左侧原图；
- 红线 = Front 起点；
- 黄线 = 安全边距/实际切线；
- 右侧实时预览；
- 原图不修改；
- 覆盖正式封面要确认。

用户实测通过。

---

# 7. Windows ffprobe / FFmpeg 相关修复

## 7.1 UTF-8

Windows 默认编码与 ffprobe JSON UTF-8 曾冲突，已明确按 UTF-8 解码。必须保留。

## 7.2 v0.3.4.3：后台命令窗口静默

用户一次加入多部影片后发现：启动扫描时每部影片都会闪一次命令行窗口。

根因：逐部调用 ffprobe，某些情况还调用 FFmpeg 自动取帧，Windows 控制台窗口会显示。

修复：

- Windows 下 ffprobe 后台运行无控制台窗口；
- FFmpeg 自动取帧也无控制台窗口；
- 不隐藏真正要显示的播放器/游戏/模拟器。

用户要求：第二阶段以后可以把真实扫描过程做成终端内部的进度表现；**第一阶段只负责静默后台命令**。

---

# 8. 影片 / 游戏封面墙性能修复

## 8.1 v0.3.4.4：影片封面墙 resize 卡顿

真实场景：用户一次加到 **17 部影片**并全部设置封面后，拖动窗口尺寸非常卡，CPU 从闲置 0% 飙到约 26%。

根因：窗口 resize 时 Qt 连续重排；旧委托在 `sizeHint()` / `paint()` 中反复：

- 从磁盘读取图片尺寸；
- 解码大图；
- `SmoothTransformation` 高质量缩放。

修复：

- 封面原始尺寸缓存；
- 180px 显示封面缩放结果缓存；
- 列表/封面变化时失效；
- resize 主要只重排，不重复解码缩放。

用户实机确认：**解决。**

## 8.2 v0.3.4.6：游戏封面墙同类修复

游戏墙原先仍是影片修复前的旧路径，因此同样缓存：

- cover dimensions；
- static scaled pixmap。

GIF 悬停预览仍逐帧运行，不把 GIF 缓存成静态死图。

用户实机确认：**成功。**

---

# 9. Game 领域定位与数据模型

游戏模块主要服务：

> 没有 Steam/GOG 等成熟平台替用户管理的本地/非平台游戏，以及用户主动加入的其他游戏对象。

正版 Steam/GOG 游戏继续由原平台负责，不做账号/商店聚合。

Game 主要字段：

```text
uuid
title
series
developer
publisher
release_date
tags
rating
favorite
description      ← 界面“作品介绍”
notes            ← 界面“我的记录”
added_at

launch_exe
launch_args
working_directory
timing_exe

cover_path
preview_gif_path
screenshot_directory

total_play_seconds
play_count
first_played_at
last_played_at
sessions[]
```

`installed` 不是永久字段，运行时推导：

```text
launch_exe 存在   → 已安装
launch_exe 不存在 → 未安装/仅档案
```

用户真实测试：

- 删除 EXE → 终端提示 EXE 有问题；
- 恢复 EXE → 原档案正常启动；
- 档案没有丢失。

---

# 10. Game 启动与计时规则

这些规则已形成稳定边界。

## 10.1 手工配置，不扫描 EXE

每款游戏明确指定：

- 启动 EXE
- 启动参数
- 工作目录
- timing EXE
- 封面/GIF/截图目录等

不扫描目录猜 EXE。

## 10.2 timing EXE 真正出现才开始

```text
终端发起 launch_exe
→ 等待 timing_exe
→ timing_exe 真正出现
→ 创建 Session / play_count +1
→ 开始计时
```

等待最多 5 分钟；没出现则不计时。

## 10.3 只统计终端发起的游戏

外部启动不接管、不补时长。

## 10.4 单活动 Session

如果 A 正在计时，误点 B：

- B 可启动；
- B 不取得计时；
- A 保持唯一 active；
- 不弹多余警告。

## 10.5 每秒 UI + 30 秒 checkpoint

- 每秒更新状态栏显示；
- 每 30 秒覆盖 SQLite + `state/active_game_session.json` 检查点；
- 不产生 heartbeat 历史；
- 正常退出精确到秒；
- 一次实际游玩 = 一条 Session。

## 10.6 主动关闭终端时

计时中关闭终端：

- 提示；
- 可取消；
- 用户坚持退出 → 结算到当前秒；
- 游戏继续运行也不会继续由终端计时；
- 不做后台服务。

### 用户最新实测

用户做过：

```text
开终端
→ 开游戏
→ 约 50 秒时主动关闭终端
→ 游戏不受影响
→ 重新打开终端
→ 这次约 50 秒游玩记录确实存在
```

重新开终端后**不会重新接上那个仍在运行中的游戏**。用户明确认为这个行为没有问题，因此不要把它误判为必须修复的 Bug。

这也意味着：

- “主动退出终端”按主动退出规则结算；
- 与真正“终端崩溃/断电，checkpoint 恢复”的语义要区分。

---

# 11. 备份 / 恢复：当前最终状态

入口只在设置。

默认可包含视觉资源。

备份包含：

- 一致性 `library.db` 快照；
- `metadata/movies/`；
- `metadata/games/`；
- settings 快照；
- 影片正式封面（勾选视觉资源时）；
- 游戏封面；
- 游戏 GIF。

不包含：

- 影片本体；
- 游戏本体；
- PS1/PS3 镜像/游戏安装内容；
- 外部 NVIDIA 截图原图；
- cache；
- logs；
- 运行中的 `active_game_session.json`。

恢复保留当前机器关键路径设置边界：影片库、播放器、FFmpeg 等机器配置不会被旧机器无脑覆盖；游戏档案内部 EXE/截图路径随档案恢复，失效时只显示不可用。

## 11.1 v0.3.4.2：恢复成功后自动重启

旧行为：

```text
恢复完成 → 软件关闭 → 用户手动再打开
```

新行为：

```text
恢复完成
→ 当前实例正常退出
→ 释放数据库 / 单实例 IPC
→ 自动启动新实例
→ 加载恢复后的数据
```

用户在 v0.3.4.7 实机恢复时确认：**软件会自己重新启动。**

## 11.2 v0.3.4.7：现存 DATA 覆盖恢复的 SQLite 文件锁

用户过去测试经常先删 DATA 再恢复，因此没有暴露问题。

后来首次在 **DATA 完整存在**时直接恢复，出现：

```text
[WinError 5] 拒绝访问
.library.db.restore.tmp → library.db
```

根因：Python `sqlite3.Connection` 的 `with connection:` 只处理 commit/rollback，不负责 close；部分读取连接可能依赖垃圾回收，Windows 因此仍锁住 `library.db`。

修复：`Database.connect()` 改为 contextmanager，在离开上下文时明确 `connection.close()`；transaction 同样显式关闭。

这是根因修复，不是给恢复覆盖加盲目 retry。

### 用户最终破坏性实测

在 DATA 不删除的真实场景：

- 执行恢复；
- 覆盖成功；
- 软件自动重启；
- 数据恢复成功；
- 《寂静岭 1》的游玩时间确实回退到备份时刻。

因此备份/恢复现在已有非常强的真实闭环验证。

---

# 12. 单实例、UAC、窗口行为

## v0.3.1：WinError 740

Rain World 目标 EXE 需要提升权限。

逻辑：

1. 正常 `Popen`；
2. 仅 `winerror == 740` 时；
3. `ShellExecuteW + runas` 触发 UAC；
4. 不让整个终端常驻管理员权限。

用户实测成功。

## v0.3.3：本次运行内保持窗口尺寸

Movie/Game 切换时保持用户当前拖出的窗口尺寸；重启仍回默认，不把尺寸写进长期设置。

## v0.3.3.1：切库小窗口弹动

原先先切页面再 QTimer 恢复尺寸，小窗口会被布局短暂撑高。

修复：切换时临时锁定当前 fixed size，布局完成立即解锁。

## v0.3.4：单实例

第二个终端启动时，在设置/SQLite/Session 初始化前检测已有实例，通过 Qt 本地 IPC 请求已有窗口恢复、置前并激活；第二进程退出。

不用 `.lock` 文件。

用户实测：成功。

---

# 13. 游戏档案 UI v0.3.4.1

旧的“查看详情”和“编辑游戏”重复，详情又重复放大封面，信息太长。

现改为单一：

> **游戏档案**

内部 tabs：

- **资料**：直接编辑；
- **游玩**：统计和 Session；
- **媒体**：主预览 + 横向缩略图胶卷。

不再在档案窗口重复巨型封面。

字段语义：

- `description` → **作品介绍**：作品本身是什么；
- `notes` → **我的记录**：个人笔记、回忆、配置、进度等。

当前功能结构属于第一阶段信息架构；第二阶段可重新做空间布局，但不要重新拆回两个详情/编辑窗口。

---

# 14. 游戏封面 / GIF / 裁剪

每款游戏：

- 1 静态封面；
- 1 GIF 预览。

悬停约 250ms 播 GIF，移出恢复封面，同一时间最多一个 GIF。

## v0.3.3：封面/GIF 清除

编辑档案可 Clear：

- 清掉档案链接；
- 删除终端自己管理的资产副本；
- **不删除用户原始源文件。**

## v0.3.4.5：游戏“裁剪导入…”

用户只需要把一张完整游戏盒图的 Front 剪出来，不需要游戏批量封面系统。

因此在游戏档案静态封面处增加：

```text
[浏览…] [裁剪导入…] [清除]
```

“裁剪导入…”直接复用影片已经验证的 Front 手动裁剪器：红线/黄线/实时预览。

- 原图不修改；
- 裁剪结果走现有 game asset 管理；
- 不新增数据库字段；
- 不做批量扫描。

---

# 15. 游戏媒体 / 截图

用户用 NVIDIA 等软件截图，终端不负责截屏。

当前媒体浏览器（v0.3.3 起）：

- 上方主预览；
- 下方横向缩略图胶卷；
- 图片和 GIF；
- 单击缩略图切主预览；
- GIF 只在主预览播放；
- 双击可调用系统默认查看器；
- 原图不复制进 cache；
- cache 只存可重建缩略图；
- 外部截图目录失效不影响档案。

用户最新说明：**已经体验很久，没什么问题。**

因此截图浏览器不再是第一阶段待验收项。

---

# 16. 模拟器：实际验证结论

当前最重要结论：

> **不要做“模拟器模块”。现有 `launch_exe + launch_args + working_directory + timing_exe` 已真实覆盖 PS1 与 PS3。**

终端只负责：

> “我要玩哪个游戏”；

模拟器负责：

> “这个平台怎么模拟和配置”。

## 16.1 DuckStation + PS1《寂静岭 1》

用户找回 DuckStation，并给只有 BIN 的汉化版补了 CUE，DuckStation 游戏库正常识别并显示封面。

已实测命令行直达，无模拟器管理 UI：

```text
DuckStation.exe -batch -nogui -fullscreen -- "...\寂静岭.cue"
```

后又改成窗口测试可使用：

```text
-batch -nogui -nofullscreen -- "...\寂静岭.cue"
```

终端内：

- launch_exe = DuckStation exe；
- args = 上述参数；
- timing_exe = DuckStation exe；
- workdir = DuckStation 目录。

用户实测：

- 终端直接打开《寂静岭》；
- DuckStation 主界面不弹；
- 正常计时；
- 退出正常结算。

DuckStation `Esc` 可打开 Pause Menu；完整模拟器设置仍可单独启动 DuckStation 调。

## 16.2 RPCS3 + PS3《God of War: Origins Collection》

用户模拟器版本：

```text
rpcs3-v0.0.39-18747-db3d9cd2_win64_msvc
```

RPCS3 自己创建的快捷方式给出：

```text
D:\Games\rpcs3-v0.0.39-18747-db3d9cd2_win64_msvc\rpcs3.exe --no-gui "%RPCS3_GAMEID%:BCAS20175"
```

工作目录：

```text
D:\Games\rpcs3-v0.0.39-18747-db3d9cd2_win64_msvc\
```

因此终端直接使用：

```text
launch_exe = rpcs3.exe
launch_args = --no-gui "%RPCS3_GAMEID%:BCAS20175"
timing_exe = rpcs3.exe
```

第一次测试出现：

```text
Another instance of RPCS3 is running
RPCS3.buf still exists
```

根因不是终端启动两次。终端只启动一次，timing 逻辑只枚举进程。

实际原因：RPCS3 之前的进程仍在任务管理器后台，因 `--no-gui` 没有任务栏/托盘可见 UI。

用户关闭旧进程，并设置 RPCS3 的：

> **Exit RPCS3 when process finishes**

之后实测：

- 终端直达游戏；
- 正常运行；
- 正常计时；
- 退出后正常结束；
- 没有终端适配代码需求。

## 16.3 模拟器结论

至今真实覆盖：

```text
PC 原生游戏
MOD Launcher + 原版 EXE timing
PS1 DuckStation + CUE
PS3 RPCS3 + Game ID
```

全部共用同一 Game 启动模型。

因此不要因“支持模拟器”去做：

- ROM 扫描；
- Core 管理；
- 通用模拟器设置 UI；
- RetroArch 类前端。

以后碰到其他模拟器，只针对具体游戏先试命令行；现有模型不足时再最小扩展。

---

# 17. 艾尔登法环 MOD Launcher 实测

用户同时安装并制作 MOD，对启动流程很清楚。

实际场景：

- launch_exe 是 MOD/Launcher；
- 真正运行的是原版游戏 EXE；
- timing_exe 指向真正游戏 EXE。

实测计时完全正常。

因此“Launcher 与 timing EXE 分离”已被真实 PC 游戏验证，不再是待测试项。

---

# 18. 第一阶段最终实测覆盖总结

截至 v0.3.4.7，用户已真实验证：

### 影片

- 大批加入影片；
- 17 部影片全部配封面；
- 播放；
- 扫描；
- GIGA 封面处理；
- resize 性能修复；
- 后台 ffprobe/FFmpeg 静默。

### 游戏

- 手动建档；
- 封面/GIF；
- 游戏档案；
- 启动；
- UAC；
- 实时计时；
- Session；
- EXE 删除/恢复；
- Launcher/timing 分离；
- PS1；
- PS3；
- 游戏墙 resize 缓存；
- 截图浏览长期使用。

### 数据

- 现存 DATA 覆盖恢复；
- 恢复后自动重启；
- 游戏时间回退证明备份状态真实恢复；
- 删除 `library.db` 后 SQLite 自动重建，无明显数据丢失；
- 主动关闭终端时 Session 记录符合设计。

因此第一阶段不再有明确功能缺口。

---

# 19. v0.3.3 → v0.3.4.7 版本历史

## v0.3.3

第一阶段易用性收尾：

- 游戏媒体：主预览 + 横向胶卷；
- 同次运行切库保持窗口尺寸；
- 游戏封面/GIF Clear。

## v0.3.3.1

修复小窗口快速切库垂直弹动。

## v0.3.4

单实例保护；用户实测成功。

## v0.3.4.1

游戏档案界面收口：

- 查看详情/编辑合一；
- 资料/游玩/媒体 tabs；
- notes → 我的记录；
- 新增 description → 作品介绍；
- schema v3。

## v0.3.4.2

备份恢复完成后自动重启。

## v0.3.4.3

Windows 后台 ffprobe/FFmpeg 命令窗口静默。

## v0.3.4.4

影片封面墙 resize 缓存性能修复；用户确认解决。

## v0.3.4.5

游戏档案增加“裁剪导入…”，复用影片 Front 裁剪器。

## v0.3.4.6

游戏封面墙 resize 缓存性能修复；用户确认成功。

## v0.3.4.7

SQLite connection 显式关闭，解决 DATA 存在时备份恢复覆盖 `library.db` 的 Windows 文件锁。

用户最终恢复测试：**成功，自动重启，数据/游玩时间回到备份状态。**

---

# 20. 当前自动验证基线

2026-08-18 从最新 v0.3.4.7 完整包重新解压并运行：

```bash
python -m pytest -q
python -m pytest -q tests/test_python_compatibility.py
python -m compileall -q app tests
```

结果：

```text
166 passed, 4 skipped
Python compatibility test: 2 passed
compileall: passed
```

4 skipped 的历史原因：当前构建环境无 PySide6，真正依赖 Qt 的 UI 测试不能在这里执行。

**注意：** 当前环境没有必要声称“真实 Windows UI 自动测试过”。Windows UI 行为以用户实机反馈为最高依据。

历史关键基线：

- v0.2.7：85 passed, 4 skipped
- v0.3.0：134 passed, 4 skipped
- v0.3.1：136 passed, 4 skipped
- v0.3.2：137 passed, 4 skipped
- v0.3.3：141 passed, 4 skipped
- v0.3.4：146 passed, 4 skipped
- v0.3.4.1：154 passed, 4 skipped
- v0.3.4.4：162 passed, 4 skipped
- v0.3.4.5：163 passed, 4 skipped
- v0.3.4.6：165 passed, 4 skipped
- v0.3.4.7：166 passed, 4 skipped

---

# 21. 第一阶段冻结的含义

“冻结”不等于声称永远没有 Bug。

冻结意味着：

- 不再为了找功能而找功能；
- 不因为第二阶段看起来还不酷就重做第一阶段数据/业务模型；
- 日常使用出现真实 Bug，可以继续 v0.3.4.x 小修；
- 第二阶段视觉改造应尽量只替换壳和呈现层；
- v0.3.4.7 保留为“功能基准线”，以后视觉重构出问题可与它比较。

---

# 22. 第二阶段：当前审美研究方法

用户明确说自己不懂 Y2K、拟物、扁平、Win95 等理论标签，主要靠“看起来喜不喜欢”判断。

因此第二阶段不要让用户先选美学流派。

正确方法：

> **先积累参考图 / 视频 → 拆解反复喜欢的视觉零件 → 再形成自己的视觉语言。**

不要从“这是 Y2K 还是 Frutiger Aero”开始。

用户会直接提供：

- 截图；
- 视频片段；
- 某个软件/主机界面；
- 某个菜单/动画。

开发助手负责拆：

- 空间结构；
- 比例；
- 材质；
- 光影；
- 信息密度；
- 选中逻辑；
- 动画；
- 哪些适合本终端；
- 哪些只是电视/手柄 UI，不应照搬。

---

# 23. 第二阶段当前审美线索

## 23.1 第七世代主机 UI / 空间感

用户非常熟悉并喜欢 PSP/PS3 时代的软件/硬件设计，也提到 Wii 上类似 WiiFlow/USB Loader GX 的体验。

反复出现的偏好：

- PS3/PSP XMB 的轴向空间导航；
- 缓慢流动的背景光带/波纹；
- UI 留白；
- 当前焦点明确；
- 内容不像网页卡片，而像空间里的“对象”；
- WiiFlow/Cover Flow 中封面像实体盒子；
- 导航是“沿轴移动”，而不是普通后台页面。

用户认为 PS4 相比之下更“大白话”：游戏基本一条横轴列完；并强烈不喜欢 PS5 删除主题系统这一类“机器个性减少”的变化。

当前理解：

> 用户喜欢的未必是某个固定“美学流派”，而是**主机 UI 的系统人格、空间感、环境感与个性化**。

## 23.2 PC 边界

用户也明确指出：PC 软件天然没有主机系统那种真正“进入感”。Windows 中软件通常就是点击后弹出一个窗口。

因此第二阶段不要：

- 为了沉浸强制全屏；
- 假装接管 Windows；
- 强制等待长启动动画；
- 牺牲正常窗口操作。

更合理目标：

> **外面仍是 Windows；但这个窗口内部有自己的世界和系统人格。**

## 23.3 Cyberpunk 2077 的位置

早期用户喜欢 2077 的 UI 动画：扫描、装配、锁定、解码、视觉加载。

目前更合理的定位：

- **PS3/PSP/Wii 类空间逻辑**作为骨架参考；
- **透明/玻璃材质**作为当前壳体方向；
- **2077**更适合以后作为动效语言，而不是把整个界面做成满屏 HUD。

动画以后可以纯装饰，不必对应真实加载，但不能拖慢程序；未来可考虑 full / simplified / off。

---

# 24. 第二阶段第一版“地基”历史构想（已被后续路线替代）

这是用户在 2026-08-18 进入第二阶段前提出的**历史主壳方向**。  
**2026-08-19 更新：此路线中的 Windows 10 外窗玻璃/透明圆角部分已经经过多轮原型后明确暂缓，不再是当前实施基础。当前真实实施路线已转为“稳定 Flat Baseline → 皮肤/Theme 架构 → Flat Pro → 可选效果层”。下面内容只保留为设计历史，不得当成当前待实现规格。**

## 24.1 整体材质

- 整个软件背景改为**半透明玻璃/毛玻璃质感**；
- 右侧展开内容区域同样使用半透明玻璃质感；
- 暂不急着做复杂转场动画。

## 24.2 左侧“大轴”

最左侧是一块长方形的主类别区域，类似一根大轴。

当前大类别只有：

```text
[图标] 影片
[图标] 游戏
[图标] 设置
```

布局：

- 图标在左；
- 名字在右；
- 上下排列；
- 宽度保持较窄；
- 不是旧第一阶段那种塞很多功能的侧边栏。

## 24.3 初始状态

用户原始想法是：

> **软件刚打开时先只显示这个长方形大类别区域，不急着展示大量内容。**

点击某个大类别后：

> **在大轴右边弹出/展开一个内容区域。**

以后右侧可能承载 3D/Cover Flow 等，但**第一版仍直接复用现有海报墙**。

其他搜索、排序、筛选、添加等功能，先按当前功能位置/逻辑在新壳里找合理位置，不重新发明业务逻辑。

## 24.4 暂缓

第一版地基暂不做：

- 复杂衔接动画；
- Cover Flow；
- 3D 游戏盒 renderer；
- Hero 大背景；
- 启动仪式动画；
- 装饰性左侧角色/展示舱；
- 大规模主题系统。

先把：

> **玻璃材质 + 大轴 + 子内容区域结构**

做稳，再向后扩展。

---

# 25. 第二阶段尚未定死的内容

不要把早期讨论当成最终需求。

仍需要继续参考积累和设计的包括：

- 真实 Windows Acrylic/Mica 还是 Qt 内伪玻璃；
- 背景颜色/透明度/模糊程度；
- 大轴精确宽度、图标语言、选中态；
- 内容区展开方式；
- 影片/游戏主墙如何在新壳里响应窗口大小；
- Wall / Flow / Archive 是否全部落地；
- 3D 游戏盒 front/spine/back 如何生成；
- Hero 是否用于影片/游戏详情；
- 主题系统到什么程度；
- 音效是否值得加入；
- 正式品牌名/Logo/EXE/icon；
- 启动动画长度/关闭选项。

目前正确状态是：**继续看参考，不急于理论化。**

---

# 26. 当前明确不做 / 不要擅自扩展

## Movie

- 内置播放器；
- 转码；
- 大型在线刮削；
- GIGA 全站离线数据库；
- 云同步；
- 多用户；
- 网络访问；
- 手机端；
- AI 自动分类。

## Game

- Steam/GOG/Epic 账号聚合；
- 社交；
- 商店；
- 推荐相似游戏；
- 官方成就 Hook；
- Overlay；
- 软件截图热键；
- 输入/window hook；
- 注入；
- 云存档；
- Mod 管理；
- 模拟器核心/ROM 扫描；
- RetroArch 类统一前端。

## 架构

- 不建万能 `resources` 表；
- 不把 Movie/Game 强行统一模型；
- 不因为第二阶段改 UI 而重写可靠的数据层。

---

# 27. 开发/交付流程

当前项目目录在这套会话环境中**没有 Git 仓库**。

过去使用：

- 复制独立工作目录做隔离；
- TDD：先失败测试再最小实现；
- 完整 pytest；
- compatibility 测试；
- compileall；
- 补丁覆盖旧版本后再测试；
- ZIP integrity；
- 排除 `__pycache__` / `.pytest_cache` / `.pyc`。

不要声称 Git commit/branch/worktree 已存在。

用户喜欢交付：

1. 小补丁 ZIP；
2. 同版本最新完整 ZIP 作为保险；
3. 没加依赖就不要让用户重装依赖。

---

# 28. 下一位开发助手最容易犯的错误

1. **误以为第一阶段还有大量功能要补。** 现在已冻结，真实 Bug 才继续修。
2. **重构万能 Resource。** 不要。
3. **把第二阶段变成“给 Qt 换套 QSS 皮肤”。** 用户要的是空间、材质和软件人格，不只是换颜色。
4. **反过来过度模拟主机。** PC 仍是窗口软件，不强制全屏，不牺牲窗口便利。
5. **把左侧“大轴”理解成旧式功能侧边栏。** 它只负责一级类别：影片/游戏/设置；低频功能不能重新塞满。
6. **默认软件启动就一定展示海报墙。** 用户最新主壳构想是初始先只显示大轴，点类别再展开右侧；这点尚在设计中，实施前应再次确认，不要擅自改成默认选中。
7. **把 2077 当全部视觉风格。** 目前更像动效参考。
8. **为了 3D/Cover Flow 先重写现有 Grid。** 第二阶段第一版仍先复用成熟海报墙。
9. **把模拟器当新模块。** DuckStation/RPCS3 已证明通用启动模型足够。
10. **把 RPCS3 残留问题归咎终端重复启动。** 已查清是模拟器进程/设置问题。
11. **认为游戏主动退出终端后必须续接仍运行的游戏。** 用户实测并明确接受当前“不续接”的行为。
12. **认为截图浏览器还没验收。** 用户已经长期使用，没问题。
13. **恢复时删 DATA 才算正确测试。** v0.3.4.7 已支持 DATA 完整存在时直接覆盖恢复并实测成功。
14. **忘记封面墙缓存。** Movie/Game resize 性能已经真实修复，不要退回每 paint 解码缩放。
15. **忘记 ffprobe/FFmpeg Windows 无控制台窗口。** 不要回归黑框闪烁。

---

# 29. 最新交付物

第一阶段冻结基线完整包仍需长期保留：

```text
本地资源终端_MVP_v0.3.4.7_第一阶段冻结基线完整版.zip
```

第一阶段冻结代码版本：

```text
0.3.4.7
```

当前第二阶段最新交付：

```text
本地资源终端_v0.4.1.8_游戏档案内容页_累计覆盖包.zip
本地资源终端_v0.4.1.8_游戏档案内容页_完整源码.zip
```

当前第二阶段代码版本：

```text
0.4.1.8
```

本交接日志：

```text
本地资源终端_开发交接日志_2026-08-19.md
```

**交接原则：** v0.3.4.7 是功能冻结参照，不要拿它覆盖当前开发线；继续第二阶段应从 v0.4.1.8 完整源码起步。
---

# 30. 下一次对话 / 开发建议（2026-08-19 当前）

当前不要重新开启 Windows 10 外窗玻璃实验，也不要回头重做已稳定的海报墙、计时、备份恢复等第一阶段业务。

下一位开发助手建议按以下顺序接手：

1. 以 **v0.4.1.8 完整源码**作为当前第二阶段开发基准；
2. 保留 **v0.3.4.7** 作为第一阶段功能冻结回退线；
3. 用户已确认 v0.4.1.8 游戏档案整页方向“总体不错”，下一步优先根据实机截图继续磨游戏档案页，而不是先扩大到影片；
4. 一个已明确但尚未实现的小交互：**游戏海报墙双击应从“直接启动游戏”改为“打开游戏档案”；启动游戏只由档案页内“启动游戏”按钮负责**；
5. 游戏档案 Hero 不支持视频。用户明确担心本地数据膨胀，当前只接受图片/GIF；
6. 影片档案不要强行套游戏档案骨架，影片展示内容更单一，后续应独立设计；
7. Flat Pro 主壳、Identity Shell、极简内容区当前均应视为已实装方向，不要退回旧式大工具栏；
8. 搜索/分类/排序已经被收进底部放大镜入口，低频功能不要重新常驻占据顶部；
9. 添加游戏已经收为底部低调 `+`，不要恢复大蓝色“添加游戏”块；
10. 用户会用真实 Windows 截图直接评价视觉。**不要在用户要求“更新/做一版”时用生成图代替实际代码更新**；除非用户明确要求视觉概念图，否则优先直接改程序；
11. 用户常用“来吧 / 做吧 / 更新吧”表示已经批准当前方案，应进入实际实现，不要反复复述同一设计；
12. 每轮交付继续保留：累计覆盖包 + 同版本完整源码包 + 完整 pytest/compileall/ZIP integrity/覆盖一致性验证。

---

# 31. 一句话当前状态

> **v0.3.4.7 仍是第一阶段功能冻结基线；第二阶段已实际推进到 v0.4.1.8：主壳已从高风险 Windows 10 外窗玻璃实验转为稳定 Flat/Theme/Skin 架构，Flat Pro、Identity Shell、极简海报墙内容区已落地，游戏档案已从弹窗升级为占据内容区的独立 Hero/媒体/资料页面；下一步应继续基于用户 Windows 实机反馈打磨，而不是重新开放第一阶段业务或恢复旧玻璃主壳路线。**

---

# 32. 2026-08-19：第二阶段路线正式转向

旧 Phase 2.1 视觉冻结稿曾把方向放在：

```text
Identity Shell → Main Rail → Content Deck
+ 半透明/玻璃/圆角外窗
```

实际原型阶段在 Windows 10 上连续遇到：

- 伪玻璃只是透明白块，不是真模糊；
- 透明圆角窗口出现矩形幽灵角；
- resize 时残留矩形材质/白块；
- 指针捕获、拖动、resize 几何行为不稳定；
- PyWinRT Composition 有环境/interop 问题；
- C#/Win32 和纯 Win32 原型仍存在透明/resize 边界问题。

结论：

> **Windows 10 外窗真实玻璃/透明圆角不再作为第二阶段地基。**

当前路线改为：

```text
第一阶段稳定业务
        ↓
稳定 UI Component Layer
        ↓
Theme / Skin tokens + assets
        ↓
Flat Pro 等正式皮肤
        ↓
可选 Effects（以后再考虑 blur / animation / 3D）
```

关键原则：

- 高级效果不得成为业务与主壳结构的前提；
- 正常矩形窗口完全可接受；
- 可以使用软件自己的 titlebar/chrome；
- 主题主要控制颜色、字体、图标、边框、圆角、间距、背景、hover/selected；
- 第一阶段不追求“万能皮肤引擎”，不允许一个皮肤随意重排全部布局；
- Flat Dark / Flat Light / Flat Pro 均通过统一 Theme Registry 管理；
- 主题保存后重启生效的模式稳定可靠，当前不追求热切换。

---

# 33. Flat Baseline / Theme / Skin 当前状态

## 33.1 Flat Baseline

v0.4.0.x 期间完成了：

- 旧 Widgets 界面向统一 Flat 视觉迁移；
- 游戏墙空白问题修复（delegate 缺少 Qt 绘制类 import）；
- 游戏档案媒体页 QLabel/pixmap sizeHint 导致对话框持续长大的问题修复；
- Settings 改为左侧分类导航 + 右侧内容页；
- Add Game 最终简化为单页：
  - 游戏名称；
  - 启动 EXE；
  - 计时 EXE；
  - 评分；
- Add Game 不再显示 series/developer/publisher/release_date/tags/favorite/notes 等低频字段，但底层 schema 字段保留；
- `description` 仍是“作品介绍”；
- `notes` 仍是“我的记录”。

## 33.2 Theme Registry

从 v0.4.0.7 起逐步稳定：

- Flat Dark；
- Flat Light；
- Flat Pro；
- Theme Registry 为主题唯一来源；
- Settings 中主题列表从 Registry 动态生成；
- 无效 theme id 安全回退；
- 旧 settings 无 theme 字段安全回退到 Flat Dark；
- 主题尺寸指标（radii/spacing/control heights/sidebar metrics）进入 token；
- 主题资产接口存在；
- 已有测试证明可注册第三主题；
- 备份包含 settings，因此主题选择随设置备份。

## 33.3 Flat Pro

Flat Pro 当前是主要设计线：

- graphite / 深色、低对比；
- 软件自绘 titlebar；
- 矩形 opaque frameless window，不做外窗透明玻璃；
- titlebar 显示“本地资源终端 + 版本”；
- 最小化使用短 `-`；
- 最大化 `□ / ❐`；
- 关闭 `×`；
- titlebar 空白处使用 `startSystemMove()`；
- 双击标题区域切换最大化；
- Windows `WM_NCHITTEST` 处理边缘 resize。

左侧导航选中态经过多轮实机比较：

- v0.4.1.1 “pit/坑”效果：拟物感强但语义不对；
- v0.4.1.2 “pressed card”看起来偏凸；
- v0.4.1.3 起改为 **low-contrast sunken card**；
- v0.4.1.4 继续细化压痕、色差、图标强调；
- 当前原则：无外部阴影、轻微沉降、蓝色选中图标、白色文字，不追求重拟物。

---

# 34. Identity Shell 当前状态

Identity Shell 已实装，但不是在线账号系统。

定位：

> **单机、本地、单身份的“进入自己的收藏空间”仪式层。**

只支持：

- username（必填）；
- avatar（可选）；
- avatar frame（可选）。

明确不做：

- 密码；
- 邮箱；
- 多用户；
- 云账号；
- token；
- 社交。

行为：

1. 每次启动先停在全窗口 Identity Entry；
2. 点击头像/身份区域进入主界面；
3. 进入后身份缩成左上角的小“身份房间”；
4. 点击左上身份房间可编辑 identity；
5. Identity 数据独立于 Movie/Game DB；
6. 自动扫描在进入身份之后才启动；
7. backup v3 已包含 identity；
8. 恢复旧 v1/v2 无 identity 时保留当前 identity。

曾发现旧视觉实验留下的 identity 会被新版本继续读取。根因是内部兼容标识 `LocalMovieManager` 的用户配置目录未变，这是预期的兼容结果，不代表 Movie/Game 数据污染。用户目前接受，不继续处理。

---

# 35. v0.4.1.x Flat Pro 主界面演进

## v0.4.1.0

Flat Pro Preview 1：

- 新增 Flat Pro；
- 自绘软件 titlebar；
- frameless + native resize；
- 第一版导航选中态。

## v0.4.1.1

- 加强导航“pit”效果；
- System/Settings 移到 Media Library 下方；
- 移除左下重复版本信息；
- 改进 pressed state。

## v0.4.1.2

- 导航尝试 pressed thin card；
- 用户实机认为偏凸；
- 最小化 glyph 改成短 `-`。

## v0.4.1.3

- Flat Pro 改为 `sunken_card`；
- 选中 icon 使用 accent 蓝；
- selected text 白色；
- 无外阴影；
- 自定义 paint 轻微内压痕。

## v0.4.1.4

继续磨选中按键：

- 适度提高选中卡片与主轴背景色差；
- 顶/左压痕更清楚；
- 下/右反光进一步减弱；
- 不回到 pit 或凸卡片。

用户实机确认这一轮整体方向可继续。

## v0.4.1.5

内容区第一次 Flat Pro Polish：

- 标题/数量层级整理；
- 搜索、筛选、排序、添加按钮统一；
- 添加按钮蓝色降存在感；
- poster selected 边框更细；
- 旧 Qt `QStatusBar` 隐藏；
- 状态信息迁到内容区底部自己的状态条；
- sidebar/content separator 变弱。

## v0.4.1.6

用户认为上方“游戏库/影片库 + 数量”和底部数量重复，因此：

- 删除内容区顶部“游戏库/影片库”；
- 删除顶部“共 N 个/部”；
- 数量只保留底部；
- 工具栏直接上提；
- Movie/Game 同一逻辑。

## v0.4.1.7：极简内容区

用户进一步提出“最大限度简化界面”。

最终：

- 顶部常驻搜索/筛选/排序/添加工具栏全部撤掉；
- 封面墙成为主要内容；
- 底部右侧使用 **放大镜** 作为搜索/分类/排序入口；
- 用户明确认为搜索、排序、分类本质都是“找东西”，因此不使用语义模糊的 `...`；
- 添加游戏改为底部低调 `+`；
- 底部左侧数量图标化：
  - 游戏：FC 卡带语义图标；
  - 影片：胶片/影片图标；
- 影片右键“查看详情”改名为 **“影片档案”**。

当前主界面视觉原则：

> **海报墙负责展示；低频工具藏起来；底部状态条负责数量和少量操作；不要重新在顶部堆满后台式控件。**

---

# 36. v0.4.1.8：游戏档案从弹窗升级为独立内容页

这是当前最新的主要结构变化。

旧 v0.3.4.1 的“游戏档案”是独立 dialog/tabs。  
v0.4.1.8 开始：

> **游戏档案是主窗口右侧内容区中的一整页，不再是独立弹窗。**

当前行为：

- 从游戏海报墙进入游戏档案；
- 左上角 `←` 返回海报墙；
- 左侧主轴/Identity Shell 保留；
- 档案页占据整个右侧内容区；
- 不再重复显示海报墙里已经出现过的大封面；
- 顶部使用横向 Hero；
- Hero 下方/旁边组织作品介绍、媒体、我的记录、档案资料、游玩统计、最近 Session；
- `启动游戏` 是档案页里的明确主操作；
- 原成熟编辑 dialog 没有删除，仍由“编辑档案”进入，负责详细字段编辑。

用户 2026-08-19 Windows 实机反馈：

> **“总得来说，还是不错的。”**

因此这条结构方向可以继续磨，不应退回旧弹窗作为主体验。

---

# 37. 游戏档案 Hero / 媒体规则

用户明确拒绝把视频作为档案背景：

> 视频会让本地数据体积膨胀，因此 **不要视频，用 GIF 代替**。

当前 Hero 仅接受：

- JPG/JPEG；
- PNG；
- WebP；
- BMP；
- GIF。

不支持视频。

当前 Hero 回退优先级：

```text
手动指定的专属档案展示媒体
→ 已有 preview GIF
→ screenshot directory 中可用的图片/GIF
→ Theme 背景
```

专属 Hero：

- 会复制进终端自己管理的 `game_assets/archive/`；
- 源文件之后移动/删除不影响终端副本；
- 新字段：`archive_media_path`；
- SQLite schema 在 v0.4.1.8 升为 **4**；
- JSON 永久档案同步保存该字段。

媒体区：

- 显示现有截图/GIF；
- 单击可临时切换 Hero；
- 双击仍用系统默认查看器打开原图；
- GIF 可循环播放；
- 外部 screenshot directory 原图仍不复制、不删除；
- 用户在 v0.4.1.8 实机看到截图自动成为 Hero，确认“还行”，当前无需改掉这个自动回退行为。

视觉上 Hero 会对档案内容区做轻微综合色调影响，但这是 **Qt/软件内部背景效果**，不是重新启用 Windows 外窗玻璃。

---

# 38. v0.4.1.8 数据与备份变化

新增永久字段：

```text
archive_media_path
```

新增终端管理视觉资源目录：

```text
Data/
└─ game_assets/
   ├─ covers/
   ├─ previews/
   └─ archive/
```

当备份启用视觉资源时，游戏专属档案 Hero 也进入备份。

恢复兼容：

- 新备份可恢复 archive Hero；
- 旧 v1/v2/v3 备份没有 archive 目录时不报错；
- 旧档案缺少 `archive_media_path` 时为空值回退；
- JSON 仍是永久档案；
- SQLite 仍是可重建索引/运行层。

游戏档案页打开期间，如果一次游玩结束：

- 累计时长刷新；
- play count/最近游玩信息刷新；
- 最近 Session 可同步显示；
- 不要求退出档案再重进。

---

# 39. 当前已明确但尚未实现：游戏双击行为

截至 **v0.4.1.8**：

- 历史行为仍是：游戏海报墙双击 → 启动游戏。

用户在看到新独立档案页后提出：

> **双击游戏封面应改成打开“游戏档案”，不要直接启动。**

建议下一小版本落实为：

```text
单击游戏封面 → 选择
双击游戏封面 → 打开游戏档案
右键 → 档案/管理菜单
档案页“启动游戏” → 真正启动
```

这样可以避免误双击启动，也让“进入游戏空间 → 再启动游戏”的第二阶段交互更完整。

**注意：这一条截至本日志更新时尚未实现，不要误写成 v0.4.1.8 已有功能。**

---

# 40. 影片档案当前边界

影片右键入口已改名为：

> **影片档案**

但影片档案目前没有跟随游戏做整页 Hero 骨架。

用户明确认为：

> 影片的展示内容不像游戏一样多种多样，因此**不适合同一骨架**。

所以后续：

- 不要为了统一而复制游戏档案；
- 影片档案应基于影片自己的内容（影片信息、封面、播放/观看记录、备注等）独立设计；
- 在游戏档案方向稳定之前，不急着扩张影片档案重构。

---

# 41. 当前版本 / 验证 / 交付基线

## 第一阶段冻结线

```text
v0.3.4.7
```

用途：

- 功能可靠性参照；
- 出现第二阶段 UI 回归时对照；
- 不再作为当前视觉开发源码。

## 当前第二阶段开发线

```text
v0.4.1.8
```

当前完整验证结果：

```text
273 passed, 4 skipped
compileall: passed
```

4 个 skipped 的原因仍是当前打包环境没有 PySide6，不能声称 Linux 构建环境自动验证了真实 Qt/Windows UI。

v0.4.1.8 打包时还做过：

- 完整源码重新解压后运行测试；
- `python -m compileall -q app tests`；
- 累计覆盖包套回 v0.3.4.7 冻结基线；
- 运行文件逐项比对一致；
- ZIP integrity 检查；
- 排除 `__pycache__` / `.pytest_cache` / `.pyc`。

用户 Windows 实机已实际打开并截图确认 v0.4.1.8 游戏档案页可用。

最新交付物：

```text
本地资源终端_v0.4.1.8_游戏档案内容页_累计覆盖包.zip
本地资源终端_v0.4.1.8_游戏档案内容页_完整源码.zip
```

---

# 42. 2026-08-19 交接强约束补充

下一位开发助手尤其不要犯这些新错误：

1. **不要把旧“玻璃主壳冻结稿”继续当现行规格。** 外窗玻璃路线已经暂缓。
2. **不要因为 Steam/GOG 已存在就劝用户停止项目。** 当前产品价值不在商店/账号聚合，而在完全本地、可控、可修改、可自定义成用户自己的私人收藏终端。
3. **不要给游戏档案加视频背景。** 用户明确拒绝，GIF 足够。
4. **不要把游戏 Hero 再改回巨大封面。** 海报墙已经承担封面浏览，档案页需要的是横向展示空间。
5. **不要把游戏档案重新拆成“查看详情 + 编辑游戏”两个主入口。** 当前整页档案负责浏览/展示，“编辑档案”只是次级编辑入口。
6. **不要把影片档案硬套游戏骨架。**
7. **不要重新把搜索/排序/分类/添加做成顶部常驻大工具栏。**
8. **不要删除 Flat Dark/Flat Light 来只保留 Flat Pro。** Theme/Skin 架构需要保留多主题回退。
9. **不要破坏 v0.3.4.7 已验证的数据、计时、备份恢复、封面墙缓存、FFmpeg 静默、模拟器直启等成熟业务。**
10. **用户说“做吧/更新吧”时优先实际改代码并交付，不要再用生成图代替实现。**

---

# 43. 最新一句话交接

> **现在的“本地资源终端”已经从第一阶段的可靠本地 Movie/Game 管理器，进入了真正的第二阶段产品塑形：Flat Pro 与 Theme/Skin 成为稳定外壳，Identity Shell 建立私人入口，海报墙内容区被极简化，游戏档案开始成为每个游戏自己的独立展示空间；继续开发应围绕真实 Windows 实机截图逐轮打磨这套“用户可以完全改成自己样子”的私人本地收藏终端。**
